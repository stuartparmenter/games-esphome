[project]
name = "games-esphome"
version = "0.0.1"
description = "Game Runner for ESPHome"
readme = "README.md"
requires-python = ">=3.11"

[dependency-groups]
dev = [
    "ruff>=0.15.15",
    "clang-format>=22.1.5",
    "clang-tidy>=22.1.0.1",
]
