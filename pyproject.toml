[project]
name = "games-esphome"
version = "0.1.0"
description = "Game Runner for ESPHome"
readme = "README.md"
requires-python = ">=3.11"

[dependency-groups]
dev = [
    "ruff>=0.15.21",
    "clang-format>=22.1.8",
    "clang-tidy>=22.1.7",
]
